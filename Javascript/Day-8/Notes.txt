1. Loops in JavaScript

    Loops are used to repeat a block of code multiple times.

    Types:

    i. for loop:

        - Used when the number of iterations is known.

            Example:
            let i;
            for (i = 0; i < 5; i++) {
                console.log(i);
            }
            i=1  t/f  ""   i++

    ii. while loop:

        - Runs as long as the condition is true.

            Example:
            let i = 0;
            while (i < 5) {
                console.log(i);
                i++;
            }

    iii. do-while loop:

        Runs at least once, then checks the condition.

            Example:
            let i = 0;
            do {
                console.log(i);
                i++;
            } while (i < 5);


2. Functions in JavaScript

   - Functions are reusable blocks of code.

        Example:
            function greet() {
            console.log("Hello Aadit");
            }
            greet();

    - With parameters:
        function add(a, b) {
        return a + b;
        }

        console.log(add(2, 3)); // 5


3. Math functions

    . Math.ceil()
    . Math.floor()
    . Math.round()
    . Math.trunc()
    . .toFixed()

    . Math.min()
    . Math.max()

    . Math.sign()
    . Math.abs()


    . Math.pow()
    . Math.sqrt()
    . Math.cbrt()
    . Math.PI

    . Math.random()
































🔥 Fresher Interview Questions (Very Important)

1. What is the difference between == and ===?
2. What is a function?
3. What is the return keyword?
4. What are operators in JavaScript?
5. What is an if-else statement?
6. What is the DOM?
7. What is an event in JavaScript?
8. What is the difference between function and variable?
9. What is the use of return in functions?
10. What is a parameter and argument?
11. What is short-circuiting in logical operators?
12. What happens if we don’t use return in a function?
13. What is NaN in JavaScript?
14. What is typeof operator?
15. What is the difference between null, undefined, and NaN?



🎯 Practice Tasks (Do These)

1. Write a program to check if a number is even or odd
2. Write a function to add two numbers
3. Check if a number is greater than 50
4. Print numbers from 1 to 10 using console.log
5. Create a button and show alert on click


