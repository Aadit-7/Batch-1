1. What are data types in JavaScript?

    Data types define what kind of data a variable holds.

    Main types:

    i. String → text
        let name = "Aadit";

    ii. Number → numbers
        let age = 23;

    iii. Boolean → true/false
         let isStudent = true;

    iv. Null → empty value (intentionally)
        let x = null;

    v. Undefined → value not assigned
        let y;

    vi. Object → collection of data
        let person =
        {
            name: "Aadit",
            age: 23
        };

    vii. Array → list of values
        let arr = [1, 2, 3];


2. How to see output?

    console.log   → Normal output
    console.table → Table format data
    console.warn  → Warning message
    console.error → Error message
    console.clear → Clears console


3. Operators in JavaScript

    Operators are used to perform operations on values.

    Types:

    i. Arithmetic Operators:

        Addition (+)
        Subtraction (-)
        Multiplication (*)
        Division (/)
        Modulus (%)


